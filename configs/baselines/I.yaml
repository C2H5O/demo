base_config: E.yaml
experiment:
  name: baseline_I_affine_disparity_distillation
  baseline_id: I
  training_required: true
  implementation_status: ready_for_server_dry_run
loss:
  depth_mode: clip_shared_affine_disparity
  depth_robust_loss: smooth_l1
  affine_detach: true
training:
  output_dir: ./outputs/baseline_I
  resume: null
vda_evaluation:
  checkpoint: ./outputs/baseline_I/last.pt
  output: ./outputs/baseline_I/evaluation_test.json
visualization:
  output_dir: ./outputs/baseline_I/visualization
