base_config: _H_stride_common.yaml

experiment:
  name: baseline_H_layer_stride_C

vda_evaluation:
  output: ./outputs/baseline_H/output_C/evaluation.json

visualization:
  output_dir: ./outputs/baseline_H/output_C/visualization

kv_sampling:
  layer_policy:
    block5: spatial
    block7: temporal
    block9: spatial
    block11: temporal
