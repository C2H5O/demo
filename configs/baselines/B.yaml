base_config: _4090_common.yaml
experiment:
  name: baseline_B_pseudo-distill
  baseline_id: B
  training_required: true
  implementation_status: ready_for_server_dry_run
attention_distill:
  enabled: false
  weight: 0.0
loss:
  highlight_mode: legacy
  highlight_cone_full_angle_degrees: 10.0
  highlight_softness: 0.0005
training:
  output_dir: ./outputs/baseline_B
  resume: null
vda_evaluation:
  checkpoint: ./outputs/baseline_B/last.pt
  output: ./outputs/baseline_B/evaluation_test.json
visualization:
  output_dir: ./outputs/baseline_B/visualization
